name: Articole zilnice

on:
  schedule:
    # 06:12 UTC (~09:12 ora Romaniei) - zilnic
    - cron: "12 6 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: daily-articles
  cancel-in-progress: false

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Genereaza articole
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          ARTICLES_PER_RUN: "6"
        run: node scripts/generate-articles.mjs

      - name: Commit si push
        run: |
          git config user.name "Cleaning Journal Bot"
          git config user.email "bot@cleaningjournal.com"
          git add src/posts/
          if git diff --staged --quiet; then
            echo "Nimic nou de publicat."
          else
            git commit -m "Articole noi automate ($(date -u +%Y-%m-%d))"
            git push
          fi
